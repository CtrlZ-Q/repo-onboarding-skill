# Project Onboarding

生成时间：<YYYY-MM-DD>
仓库路径：`<path>`
深度模式：<Quick / Standard / Deep>

## 1. 项目概览

<用 2-5 句话说明这个项目是什么。只写能从 README、代码或配置确认的信息。标注证据来源。>

## 2. 技术栈

| 类别 | 识别结果 | 依据 |
|---|---|---|
| 主要语言 | <例如 TypeScript / Python / Rust> | <文件或配置依据> |
| 框架 | <例如 React / FastAPI / Next.js> | <文件或配置依据> |
| 包管理器 | <例如 pnpm / npm / uv / cargo> | <lockfile 或配置依据> |
| 运行时 | <例如 Node 20 / Python 3.12 / 未确认> | <配置或推断> |
| 测试框架 | <例如 Vitest / pytest / Jest> | <配置或依赖依据> |
| 构建工具 | <例如 Vite / Next.js / Cargo> | <配置或依赖依据> |
| 部署方式 | <如 Docker / GitHub Actions / 未确认> | <依据> |

## 3. 常用命令

### 安装依赖

```bash
<command>
```

来源：`<file>` | 状态：<已验证 / 已配置 / 仅文档 / 推测>

### 本地开发 / 启动

```bash
<command>
```

来源：`<file>` | 状态：<已验证 / 已配置 / 仅文档 / 推测>

### 测试

```bash
<command>
```

来源：`<file>` | 状态：<已验证 / 已配置 / 仅文档 / 推测>

### 构建

```bash
<command>
```

来源：`<file>` | 状态：<已验证 / 已配置 / 仅文档 / 推测>

### Lint / 类型检查

```bash
<command>
```

来源：`<file>` | 状态：<已验证 / 已配置 / 仅文档 / 推测>

## 4. 核心目录

| 路径 | 作用 | 后续改动建议 |
|---|---|---|
| `<dir>/` | <作用> | <什么任务先看这里> |

标注哪些目录是生成代码/构建产物/第三方依赖，不应手改。

## 5. 关键入口文件

| 文件 | 作用 | 依据 |
|---|---|---|
| `<file>` | <入口或关键配置作用> | <为什么认为它重要> |

## 6. 执行流

<至少追踪一条从入口到核心逻辑的具体路径。用文件名和函数名，不要写抽象概述。>

**路径示例（前端）：**
> `src/main.tsx` → `<App>` 组件 → `router.ts` 路由配置 → `/dashboard` 页面 → `useApi()` hook → `api/client.ts` 请求

**路径示例（后端）：**
> `src/main.ts` → `createServer()` → `routes/index.ts` 路由注册 → `authMiddleware` → `UserController.get()` → `UserRepository.findById()`

如果无法确认完整路径，写明已确认的部分和需要进一步阅读的文件。

## 7. 架构和数据流

<简要说明应用入口、路由、服务层、状态、数据访问、外部 API、配置之间的大致关系。>

如果无法确认，请写：

> 当前仅能确认 `<事实>`；完整业务数据流需要进一步阅读 `<文件/目录>`。

## 8. 测试与验证策略

推荐后续修改后的验证顺序：

1. <最小验证命令>
2. <相关单元/集成测试>
3. <必要时手动验证方式>

标注每条命令的状态（已验证 / 已配置 / 推测）。

## 9. 项目约定

- <包管理器约定>
- <目录约定>
- <生成代码约定>
- <迁移 / 配置 / 环境变量约定>

## 10. 风险点与不确定信息

| 类型 | 内容 | 建议 |
|---|---|---|
| 风险 | <例如存在生成代码目录> | <不要手改 / 先确认> |
| 不确定 | <例如未找到测试命令> | <需要人工确认> |
| 冲突 | <例如 README 与 lockfile 不一致> | <推荐处理> |

## 11. 给 Claude 的工作建议

后续在这个仓库里工作时建议：

1. <修改前先读哪些文件>
2. <优先复用哪些目录中的现有实现>
3. <修改后运行哪些命令验证>
4. <哪些文件/目录不要改>
5. <遇到不确定信息时如何向用户确认>
